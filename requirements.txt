# pile_trace.py — dépendances Python
#
# Installation :
#   python3 -m venv .env && source .env/bin/activate
#   pip install -r requirements.txt
#
# Versions minimales testées. Les versions plus récentes devraient marcher.

# Safety pour Python 3.12+ : distutils retiré de la stdlib, certaines deps
# (scikit-commpy notamment) ont des setup.py legacy qui en dépendent.
# Setuptools moderne fournit un shim distutils.
# Sur Debian/Ubuntu avec Python 3.10/3.11 : sudo apt install python3-distutils
setuptools>=65.0
wheel>=0.38

# Core numérique
numpy>=1.20
scipy>=1.7

# L14 : rastérisation glyph via TTF
Pillow>=9.0

# L7 : Milenage AKA (f1, f2-f5, make_OPc)
#   PyPI : CryptoMobile  |  import : CryptoMobile.Milenage

# L8 : codeur convolutif rate-1/3 + Viterbi decode
#   PyPI : scikit-commpy  |  import : commpy.channelcoding
scikit-commpy>=0.7

# L7 : AES-128-CTR (PDCP chiffrement/déchiffrement)
#   ATTENTION : pycryptodomex (import: Cryptodome), PAS pycryptodome (import: Crypto)
#   Les deux ne doivent jamais coexister dans le même venv.
pycryptodomex>=3.15


# ─── Optionnel : GNU Radio (L11 chaîne RF réelle) ────────────────────────────
#
# Pas pip-installable proprement (le binding Python vient avec l'install système
# de GR). Si absent, L11 fallback automatique sur scipy avec même précision.
#
# Ubuntu/Debian :  sudo apt install gnuradio gnuradio-dev
# Arch         :  sudo pacman -S gnuradio
# macOS (brew) :  brew install gnuradio
# Fedora       :  sudo dnf install gnuradio gnuradio-devel
#
# Vérification :  python -c "from gnuradio import gr; print(gr.version())"
#
# Note : gr-gsm n'est PAS requis (pipeline LTE, pas GSM). Si tu veux le
# parallèle GSM ou utiliser gr-lte pour PSS detection multi-cell :
#   sudo apt install gr-osmosdr   # SDR drivers usuels
#   git clone https://github.com/bkerler/gr-gsm  # fork maintenu
